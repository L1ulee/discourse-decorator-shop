# frozen_string_literal: true

# Development-only tooling for linting this plugin in isolation. Discourse
# itself provides the runtime gems; nothing here is loaded at plugin runtime.
source "https://rubygems.org"

group :development do
  gem "rubocop-discourse"
  gem "syntax_tree"
end
